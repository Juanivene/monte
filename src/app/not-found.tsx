import { NotFoundContent } from "@/components/shop/NotFoundContent";

export const metadata = { title: "Página no encontrada" };

export default function NotFound() {
  return <NotFoundContent bare />;
}
